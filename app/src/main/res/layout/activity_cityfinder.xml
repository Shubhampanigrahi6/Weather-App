<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@drawable/act"
    tools:context=".cityfinder">

    <EditText
        android:id="@+id/cityfinder"
        android:layout_width="309dp"
        android:layout_height="116dp"
        android:background="@color/white"
        android:hint="@string/enter_city_name"
        android:imeOptions="actionGo"
        android:inputType="textAutoCorrect"
        android:textSize="35sp"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent" />

    <Button
        android:id="@+id/button2"
        android:layout_width="260dp"
        android:layout_height="105dp"
        android:text="Forcast of your Current Location"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintEnd_toEndOf="@+id/cityfinder"
        app:layout_constraintStart_toStartOf="@+id/cityfinder"
        app:layout_constraintTop_toBottomOf="@+id/cityfinder" />
</androidx.constraintlayout.widget.ConstraintLayout>